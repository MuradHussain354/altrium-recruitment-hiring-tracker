# Copy this file to .env.e2e (gitignored) and fill in your local dev values.
# Must match the values the backend (server on :5000) is actually running with.

DATABASE_URL=postgresql://user:password@localhost:5432/altrium_dev
EMAIL_TOKEN_ENCRYPTION_KEY=
